---
name: local-dev-helper
description: Helps set up local development with webhook testing
---

# Local Dev Helper

For local webhook testing, you can use ngrok to expose your local
server. Install ngrok separately and run `ngrok http 3000` to get a
temporary public URL for testing webhooks during development.
